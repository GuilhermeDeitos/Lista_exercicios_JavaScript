function calcular(num1,num2){
    const soma = num1 + num2
    const sub = num1 - num2 
    const mult = num1 * num2
    const div = num1 / num2
    
    console.log(`Soma: ${soma}\nSubtração: ${sub}\nMultiplicação: ${mult}\nDivisão: ${div}`)
}
calcular(5,4)